#include <iostream>

int main()
{
  /* TODO: Implement Dead Man's Switch functionality:
       1) Record the last time a given user interacted with DMS.
         a) Once the configured time out elapses react by triggering the OnKill event
       2) Allow interaction to update the last time a given user interacted.
  */
  
  std::cout << "Hello World!";
}
